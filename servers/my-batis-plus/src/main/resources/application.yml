server:
  port: 28700
# 配置数据源
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/eca_mysql?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: root

# 配置日志
logging:
  level:
    root: warn
    com.zimug.boot.launch.mapper: trace